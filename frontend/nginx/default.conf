server {
    listen 80;
    server_name localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # API requests are handled by the main Nginx proxy, 
    # but we can add basic configuration here if needed.
}
